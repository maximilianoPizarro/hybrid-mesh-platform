apiVersion: skupper.io/v2alpha1
kind: Connector
metadata:
  name: kafka-{{ .Values.clusterName }}-stormshift
  namespace: service-interconnect
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  routingKey: kafka-{{ .Values.clusterName }}-stormshift
  host: factory-cluster-kafka-bootstrap.industrial-edge-stormshift-messaging.svc.cluster.local
  port: 9092
