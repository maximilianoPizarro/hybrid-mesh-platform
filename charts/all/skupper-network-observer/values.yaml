# Subchart values — deploy into Argo destination namespace (service-interconnect).
network-observer:
  fullnameOverride: skupper-network-observer
  route:
    enabled: false
  tls:
    skupperIssued: true
  # Demo: open console without basic auth on /api (workshop / RHDP labs only).
  auth:
    strategy: none
    basic:
      create: false
