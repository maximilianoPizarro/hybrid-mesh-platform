{{- /*
  Region-specific advertised bootstrap hostname resolves to the Strimzi bootstrap
  service on the spoke so local producers/consumers can follow broker metadata.
*/ -}}
{{- $cn := include "industrial-edge-tst.clusterName" . -}}
apiVersion: v1
kind: Service
metadata:
  name: dev-cluster-kafka-bootstrap-{{ $cn }}
  namespace: industrial-edge-tst-all
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ExternalName
  externalName: dev-cluster-kafka-bootstrap.industrial-edge-tst-all.svc.cluster.local
  ports:
    - name: tcp
      port: 9092
      protocol: TCP
      targetPort: 9092
