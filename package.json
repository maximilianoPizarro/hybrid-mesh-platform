{
  "name": "hybrid-mesh-platform",
  "private": true,
  "description": "Optional dev tools for workshop screenshot batch capture (not required for pattern install)",
  "type": "module",
  "scripts": {
    "capture:workshop": "node scripts/capture-workshop-screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
